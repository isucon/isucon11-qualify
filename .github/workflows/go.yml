name: ci for go
on:
  push:
    branches: [ main ]
    paths:
      - bench/**/*
      - webapp/go/**/*
      - development/**/*
      - .github/workflows/go.yml
  pull_request:
    paths:
      - bench/**/*
      - webapp/go/**/*
      - development/**/*
      - .github/workflows/go.yml

jobs:
  lint:
    name: Lint
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v2
        with:
          version: latest
          working-directory: ./webapp/go
          args: "-c ../../.github/.golangci.yml"
  test:
    name: Integration Test
    runs-on: self-hosted
    defaults:
      run:
        working-directory: development
    steps:
      - uses: actions/checkout@v2
      - name: Build Go App
        run: target=go docker-compose -f docker-compose-ci.yml up -d --build -- backend mysql-backend
      - name: Test Go App
        run: target=go docker-compose -f docker-compose-ci.yml up --build --exit-code-from apitest -- apitest
      - name: Clean up
        if: failure() || true
        run: target=go docker-compose -f docker-compose-ci.yml down
